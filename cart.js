function row(item){return `<tr><td style='display:flex;gap:10px;align-items:center'><img src='${item.thumbnail}' alt='${item.title}' style='width:64px;height:64px;border-radius:8px;border:1px solid var(--border)'><div><div style='font-weight:600'>${item.title}</div><div class='kicker'>${item.id}</div></div></td><td>${currency(item.price)}</td><td><input type='number' min='1' value='${item.qty}' style='width:80px' oninput="updateQty('${item.id}', this.value); render()"></td><td>${currency(item.price*item.qty)}</td><td><button class='btn danger' onclick="removeFromCart('${item.id}'); render()">Remove</button></td></tr>`}
function render(){const items=getCart();const tbody=document.querySelector('tbody');tbody.innerHTML=items.map(row).join('')||'<tr><td colspan=5>Your cart is empty.</td></tr>';const t=cartTotals();document.getElementById('subtotal').textContent=currency(t.subtotal);document.getElementById('shipping').textContent=currency(t.shipping);document.getElementById('total').textContent=currency(t.total);}document.addEventListener('DOMContentLoaded',render);